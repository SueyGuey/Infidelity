/**
 * types.ts
 * contains all types of actions
 * note: loadable has 3 statuses of success, loading, and error
 */

export enum Action {
	FETCH_USER_PROFILE,
	UPDATE_USER_PROFILE,
	FETCH_MARKET_DATA,
	FETCH_TRADEABLE,
	SEARCH_MARKET,
}
